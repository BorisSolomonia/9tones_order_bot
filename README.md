"# 9tones_order_bot"

## Sessions

The backend currently stores login sessions in memory. Restarting the backend logs users out, and running multiple backend instances requires sticky sessions or external session storage.
